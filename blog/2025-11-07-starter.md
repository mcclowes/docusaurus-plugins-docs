---
slug: starter
title: Introducing the plugin starter
authors: [max]
tags: [plugins, starter]
---

`docusaurus-plugin-starter` is the repo I wanted before building my first Docusaurus plugin.

<!--truncate-->

It includes a typed plugin skeleton, lifecycle hooks, client modules, a swizzlable theme component, a remark transform, tests, build scripts, and a small Docusaurus site for trying the whole thing locally. The examples are there to be cut apart. Rename the project, remove what you don't need, and keep the plumbing that would otherwise consume the first half-day.

The starter is a template rather than a runtime dependency, so it isn't registered on this site or published as a public npm package. The documentation uses a packaged archive to keep its examples reproducible.

Read the [starter docs](/docs/starter/overview) or [browse the repository](https://github.com/mcclowes/docusaurus-plugin-starter).
